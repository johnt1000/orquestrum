---
id: ARCHITECTURE-v0-as-is
project: "{PROJECT_NAME}"
mapped_at: YYYY-MM-DD
mapper_version: "1.0.0"
status: Draft  # Draft | Reviewed
---

# As-Is Architecture — {PROJECT_NAME}

> Document generated by codebase-mapper. Reflects the real state of the code at the time of mapping.
> Contains no improvement proposals — only a faithful description of what exists.

---

## 1. Technology Stack

| Dimension | Technology | Version | Source |
|-----------|-----------|---------|--------|
| Primary language | {language} | {version} | `{manifest file}` |
| Runtime | {runtime} | {version} | `{file}` |
| Primary framework | {framework} | {version} | `{file}` |
| Database | {db} | {version} | `{config file}` |
| Package manager | {npm/pip/gem/etc} | {version} | `{lockfile}` |
| Execution environment | {local/Docker/etc} | — | `{file}` |

**Notable dependencies:**

| Package | Version | Inferred purpose |
|---------|---------|-----------------|
| {package} | {version} | {what it is used for} |

---

## 2. Entry Points

### 2.1 HTTP Routes

| Method | Path | Handler / Controller | Authentication |
|--------|------|---------------------|----------------|
| {GET/POST/etc} | `{/path}` | `{file:line}` | {Yes/No/Type} |

### 2.2 Background Workers / Jobs

| Name | Trigger | Technology | File |
|------|---------|-----------|------|
| {name} | {cron/event/manual} | {bull/sidekiq/etc} | `{file}` |

### 2.3 CLI Commands

| Command | Inferred description | File |
|---------|---------------------|------|
| `{command}` | {what it does} | `{file}` |

### 2.4 Webhooks / Event Listeners

| Event | Handler | External source |
|-------|---------|----------------|
| {event} | `{file:line}` | {service} |

> If not identified: `[Not identified in code]`

---

## 3. System Components

| Component | Inferred Responsibility | Technology | Main files |
|-----------|------------------------|-----------|-----------|
| {name} | {what it does} | {tech} | `{file}` |

---

## 4. External Integrations

| Service | Type | SDK/Client used | Environment variable |
|---------|------|----------------|---------------------|
| {service} | {REST API/GraphQL/SDK/etc} | `{package}` | `{ENV_VAR}` |

> If not identified: `[Not identified in code]`

---

## 5. Data Model

### 5.1 Main Entities

```
{Entity}
├── {field}: {type}  # {inferred purpose}
├── {field}: {type}
└── ...
```

### 5.2 Relationships

| Entity A | Relationship | Entity B | Cardinality |
|----------|-------------|---------|------------|
| {A} | has_many / belongs_to / etc | {B} | 1:N / N:N |

### 5.3 Migrations / Schema

| Schema File | ORM Technology | Last migration |
|-------------|--------------|----------------|
| `{file}` | {sequelize/prisma/activerecord/etc} | `{date or name}` |

---

## 6. As-Is Diagram

```mermaid
graph TD
    Client[Client / User]
    
    subgraph API [{PROJECT_NAME} API]
        Routes[HTTP Routes]
        Controllers[Controllers]
        Services[Services]
        Models[Models]
    end
    
    subgraph Storage [Persistence]
        DB[(Database\n{type})]
    end
    
    subgraph External [External Integrations]
        Ext1[{External service 1}]
        Ext2[{External service 2}]
    end

    Client --> Routes
    Routes --> Controllers
    Controllers --> Services
    Services --> Models
    Models --> DB
    Services --> Ext1
    Services --> Ext2
```

> Adjust the diagram to reflect the actual topology found. Use `sequenceDiagram` for critical flows if more illustrative.

---

## 7. Configuration and Infrastructure

### 7.1 Environment Variables

| Variable | Inferred purpose | Required | Found in |
|----------|-----------------|----------|---------|
| `{VAR_NAME}` | {what it is for} | Yes/No | `{file}` |

### 7.2 Configuration Files

| File | Purpose |
|------|---------|
| `{file}` | {what it configures} |

### 7.3 Containerization / Deploy

| File | Technology | Note |
|------|-----------|------|
| `{Dockerfile/docker-compose.yml/etc}` | {Docker/k8s/etc} | {note} |

> If not identified: `[Not identified in code]`

---

## 8. Tests

| Type | Framework | Directory | Estimated coverage |
|------|-----------|----------|--------------------|
| Unit | {jest/rspec/pytest/etc} | `{dir}` | {high/medium/low/not identified} |
| Integration | {framework} | `{dir}` | {coverage} |
| E2E | {framework} | `{dir}` | {coverage} |

> If no tests: `[No tests identified in the codebase]`

---

## 9. Observations

> Record inconsistencies, mixed patterns, or evidence of decision changes over time.
> DO NOT propose corrections — only flag.

- ⚠️ **Observation:** {description of what was found — without judgment}
- ⚠️ **Observation:** {inconsistent pattern found}

> If no observations: `[No observations recorded]`

---

## 10. References

| Type | File |
|------|------|
| Dependency manifest | `{package.json / Gemfile / etc}` |
| Database schema | `{file}` |
| Infrastructure config | `{file}` |
| Previous documentation (if it existed) | `{file or "did not exist"}` |
