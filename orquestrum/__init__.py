"""Orquestrum — Specification-Driven Development framework for multi-agent AI.

This package provides the `orquestrum` CLI executable. The canonical framework
source (agents/, skills/, docs/, orquestrum/lib/) is unchanged — this package is
the CLI and library layer on top of the canonical framework source.

Mac and Linux only. See docs/governance/DISTRIBUTION.md.
"""
__version__ = "0.5.1"
